# Development — copy to dashboard/.env.local
# Leave NEXT_PUBLIC_API_URL empty: dashboard proxies /api/v1/* to FastAPI (no CORS issues).
# Set a full URL only when the API is on another host (production).
NEXT_PUBLIC_API_URL=
API_PROXY_URL=http://127.0.0.1:8001
